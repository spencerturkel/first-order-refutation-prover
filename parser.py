"""Formula string parsing module.
"""

from ast import Formula
from typing import Optional


def parse(formula_string: str) -> Optional[Formula]:
    """Parse the formula string into an abstract syntax tree."""
    return None if formula_string else None  # TODO:
